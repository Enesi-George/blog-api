## How to run the application

-   clone the url http
-   run migration
    php artisan migrate
-   seed user data
    php artisan db:seed
-   run the artisan server to start the project
    php artisan serve
